{
  "name": "poortaal",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.app.json && vite build --base=/poortaal/",
    "test": "node --test \"src/**/*.test.ts\" \"evals/**/*.test.ts\"",
    "eval:words": "node evals/run-word-explanation-evals.ts",
    "preview": "vite preview --base=/poortaal/"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "8.2.2"
  }
}
