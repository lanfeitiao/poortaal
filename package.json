{
  "name": "poortaal",
  "private": true,
  "scripts": {
    "build:validation": "esbuild src/zodValidationOverride.ts --bundle --format=iife --platform=browser --outfile=public/zod-validation.js",
    "dev": "tsc -p tsconfig.app.json && npm run build:validation && concurrently --kill-others \"tsc --watch -p tsconfig.app.json --preserveWatchOutput\" \"vite\"",
    "build": "tsc -p tsconfig.app.json && npm run build:validation && vite build --base=/poortaal/",
    "preview": "vite preview --base=/poortaal/"
  },
  "dependencies": {
    "zod": "4.5.4"
  },
  "devDependencies": {
    "concurrently": "10.0.5",
    "esbuild": "0.28.2",
    "typescript": "7.0.2",
    "vite": "8.2.2"
  }
}
